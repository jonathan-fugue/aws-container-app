import Lwpkg as .

import Lwpkg as .

aws-container-app:
  Package.new {
    description: "Container App and App Cluster",
    version: Version.new(0,0,0),
    dependencies: [],
    modules: [
      "AppCluster",
    ]
  }
